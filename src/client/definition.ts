/** draw.io (mxGraph XML) metadata and keyed document-body identity. */

/** Implementation identity shared by the preview metadata and its keyed body slot. */
export const DRAWIO_BODY_ID = '@guowenzhang/dsh-drawio'

/** File suffixes carrying mxGraph XML that this package renders. */
export const DRAWIO_EXTENSIONS = ['drawio'] as const
